# Быстрая шпаргалка по проекту WordMarkdownAddIn

## 🎯 Что это?
Надстройка VSTO для Word, позволяющая редактировать Markdown в панели задач с HTML-превью.

## 📁 Структура проекта

```
ThisAddIn.cs              ← ТОЧКА ВХОДА (жизненный цикл)
├── Controls/
│   └── TaskPaneControl.cs    ← UI редактора (WebView2)
├── Services/
│   ├── MarkdownRenderService.cs    ← Markdown → HTML
│   ├── MarkdownToWordFormatter.cs ← Вставка в Word
│   └── DocumentSyncService.cs      ← Синхронизация
└── MarkdownRibbon.cs      ← Лента Word
```

## 🔑 Ключевые компоненты

| Компонент | Роль | Где искать |
|-----------|------|------------|
| **ThisAddIn** | Точка входа, координатор | `ThisAddIn.cs` |
| **TaskPaneControl** | Редактор Markdown | `Controls/TaskPaneControl.cs` |
| **MarkdownRibbon** | Лента Word | `MarkdownRibbon.cs` |
| **MarkdownRenderService** | Рендеринг | `Services/MarkdownRenderService.cs` |

## 🔄 Поток данных

```
Пользователь → WebView2 (JS) → WebMessageReceived → 
MarkdownRenderService → HTML → WebView2 (превью)
```

## 💾 Хранение Markdown

- **Где**: CustomXMLParts (встроенные XML-фрагменты в документе)
- **Формат**: `<md:markdown><md:content><![CDATA[...]]></md:content></md:markdown>`
- **Методы**: `SaveMarkdownToDocument()`, `LoadMarkdownFromDocument()`

## 🛠️ Технологии

- **VSTO** — интеграция с Word
- **WebView2** — веб-компонент для редактора
- **Markdig** — парсинг Markdown
- **COM Interop** — взаимодействие с Word

## 📚 Документация

- `docs/ПУТЕВОДИТЕЛЬ_ПО_ИЗУЧЕНИЮ_ПРОЕКТА.md` — полный гайд
- `docs/ThisAddIn.md` — описание главного модуля
- `docs/UML-ClassDiagram.md` — диаграмма классов

## 🚀 Быстрый старт

1. Прочитать `README.md`
2. Изучить `ThisAddIn.cs` (метод `ThisAddIn_Startup`)
3. Посмотреть `docs/UML-ClassDiagram.md`
4. Запустить проект

## ❓ Где искать код для...

| Задача | Файл |
|--------|------|
| Инициализация | `ThisAddIn.cs` → `ThisAddIn_Startup()` |
| Редактор Markdown | `Controls/TaskPaneControl.cs` |
| Рендеринг HTML | `Services/MarkdownRenderService.cs` |
| Вставка в Word | `Services/MarkdownToWordFormatter.cs` |
| Сохранение Markdown | `ThisAddIn.cs` → `SaveMarkdownToDocument()` |
| Лента Word | `MarkdownRibbon.cs` |

## ⚠️ Известные проблемы

- Плохая обработка исключений (`catch { }`)
- Нет восстановления состояния панели при старте
- Потенциальные утечки COM-объектов

См. `docs/ThisAddIn.md` → раздел "Что нужно дописать"






