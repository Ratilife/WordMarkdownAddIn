# План реализации проекта WordMarkdownAddIn

## Общее описание проекта

Проект представляет собой надстройку VSTO для Microsoft Word с трёхпанельным интерфейсом (DOCX, Markdown, HTML) и двунаправленной синхронизацией между ними.

---

## 📋 Этап 1: Интерфейс и переключение режимов отображения

### Задача 1.1: Добавить переключение видимости Markdown/HTML в панели

**Файлы для модификации:**
- `Controls/TaskPaneControl.cs`
- JavaScript в методе `BuildHtmlShell()`

**Что нужно сделать:**

1. **Добавить кнопки/переключатели в HTML интерфейс:**
   - Кнопка "Split" (Markdown | HTML) - показывает оба одновременно
   - Кнопка "Markdown" (только Markdown-редактор)
   - Кнопка "HTML" (только HTML-превью)

2. **Реализовать JavaScript функции для переключения режимов:**
   - Функция переключения видимости элементов (изменение CSS `display`)
   - Сохранение выбранного режима в localStorage

3. **Добавить методы в TaskPaneControl для управления видимостью из C#:**
   - `SetViewMode(string mode)` - установить режим отображения
   - `GetCurrentViewMode()` - получить текущий режим

4. **Сохранять настройки пользователя:**
   - Режим отображения должен сохраняться в настройках приложения

### Задача 1.2: Улучшить стили для разных режимов отображения

**Что нужно сделать:**

1. **CSS стили для режимов:**
   - Split-view: `display: flex`, `width: 50%` для каждого
   - Markdown-only: `width: 100%`, скрыть preview
   - HTML-only: `width: 100%`, скрыть editor

2. **Анимация переключения (опционально):**
   - Плавное переключение между режимами

---

## 📋 Этап 2: Синхронизация DOCX → Markdown → HTML (Сценарий 1)

### Задача 2.1: Отслеживание изменений в Word документе

**Файлы для модификации:**
- `ThisAddIn.cs`
- Новый сервис: `Services/WordToMarkdownService.cs`

**Что нужно сделать:**

1. **Подписаться на события Word:**
   - `Application.DocumentBeforeSave` (уже есть)
   - `Application.WindowSelectionChange` - отслеживание выделения
   - `Document.ContentControlOnEnter` - при вводе текста
   - Использовать таймер/дебаунс для отслеживания изменений содержимого документа

2. **Создать сервис для отслеживания изменений:**
   - Класс `WordToMarkdownService` для мониторинга документа
   - Метод для получения текста из выделения или всего документа
   - Определение изменений через сравнение с предыдущим состоянием

3. **Парсинг текста из Word:**
   - Извлечение текста с сохранением Markdown-синтаксиса (если написан как текст)
   - Определение Markdown-синтаксиса в тексте Word

### Задача 2.2: Преобразование Word → Markdown

**Файлы для создания:**
- Новый: `Services/WordToMarkdownConverter.cs`

**Что нужно сделать:**

1. **Метод извлечения текста из Word:**
   - Получить текст из выделения или всего документа
   - Сохранить Markdown-синтаксис (если написан как текст)

2. **Метод определения Markdown-синтаксиса в тексте:**
   - Регулярные выражения для поиска `**текст**`, `*текст*`, `# Заголовок` и т.д.
   - Валидация корректности синтаксиса

3. **Автоматическое обновление редактора Markdown:**
   - При изменении Word → обновить `TaskPaneControl.SetMarkdown()`
   - Автоматический рендеринг HTML через существующий механизм

### Задача 2.3: Автоматическое обновление HTML

**Файлы для модификации:**
- `Controls/TaskPaneControl.cs` (уже реализовано частично)

**Что нужно сделать:**

1. **Убедиться, что при изменении Markdown автоматически обновляется HTML:**
   - Проверить работу механизма `mdChanged` → рендер HTML
   - Оптимизировать дебаунс для плавной работы

---

## 📋 Этап 3: Синхронизация Markdown → DOCX (Сценарий 2)

### Задача 3.1: Кнопка "Перенести в Word с форматированием"

**Файлы для модификации:**
- `MarkdownRibbon.cs`
- `MarkdownRibbon.Designer.cs`
- Новый: `Services/MarkdownToWordFormatter.cs`

**Что нужно сделать:**

1. **Добавить кнопку в Ribbon:**
   - Создать кнопку в группе "Файл" или новой группе "Синхронизация"
   - Название: "Перенести в Word" или "Применить в Word"

2. **Создать сервис MarkdownToWordFormatter:**
   - Парсинг Markdown через Markdig (уже используется)
   - Преобразование элементов Markdown в стили Word:
     - `# Заголовок` → `Word.WdBuiltinStyle.wdStyleHeading1`
     - `**текст**` → `Font.Bold = true`
     - `*текст*` → `Font.Italic = true`
     - `~~текст~~` → `Font.StrikeThrough = true`
     - Списки → `ListFormat`
     - Таблицы → `Table`
     - Ссылки → гиперссылки Word

3. **Метод ApplyMarkdownToWord():**
   - Вставка форматированного текста в Word
   - Обработка всех элементов Markdown

### Задача 3.2: Кнопка "Перенести в Word без изменений"

**Файлы для модификации:**
- `MarkdownRibbon.cs`
- `MarkdownRibbon.Designer.cs`
- `Services/MarkdownToWordFormatter.cs`

**Что нужно сделать:**

1. **Добавить кнопку в Ribbon:**
   - Кнопка для вставки Markdown как обычного текста

2. **Метод InsertMarkdownAsPlainText():**
   - Получить Markdown из редактора
   - Вставить в Word как обычный текст (без форматирования)
   - Простая вставка текста

---

## 📋 Этап 4: Форматирование Markdown прямо в Word документе

### Задача 4.1: Кнопка "Применить Markdown-разметку в Word"

**Файлы для модификации:**
- `MarkdownRibbon.cs`
- `MarkdownRibbon.Designer.cs`
- Новый: `Services/WordMarkdownFormatter.cs`

**Что нужно сделать:**

1. **Добавить кнопку в Ribbon:**
   - Создать кнопку в группе "Форматирование"
   - Название: "Применить Markdown" или "Форматировать Markdown"

2. **Создать сервис WordMarkdownFormatter:**
   - Получить выделенный текст или весь документ
   - Найти Markdown-синтаксис в тексте:
     - `**текст**` → применить жирный
     - `*текст*` → применить курсив
     - `# Заголовок` → стиль "Заголовок 1"
     - `## Заголовок` → стиль "Заголовок 2"
     - И т.д.

   - Удалить Markdown-синтаксис после форматирования
   - Применить стили Word к соответствующим частям текста

3. **Метод FormatMarkdownInWord():**
   - Основная логика форматирования
   - Поиск и замена с применением стилей

### Задача 4.2: Парсинг Markdown-синтаксиса в тексте Word

**Что нужно сделать:**

1. **Регулярные выражения для поиска:**
   - Жирный: `\*\*(.*?)\*\*`
   - Курсив: `\*(.*?)\*`
   - Зачеркнутый: `~~(.*?)~~`
   - Заголовки: `^(#{1,6})\s+(.+)$`
   - Списки: `^[\-\*\+]\s+`
   - Код: `` `(.*?)` ``
   - И другие элементы Markdown

2. **Поиск и замена:**
   - Найти все вхождения
   - Применить соответствующие стили Word
   - Удалить Markdown-синтаксис

---

## 📋 Этап 5: Оптимизация и улучшение UI

### Задача 5.1: Улучшение интерфейса панели

**Файлы для модификации:**
- `Controls/TaskPaneControl.cs` (HTML/CSS)

**Что нужно сделать:**

1. **Добавить заголовки/вкладки:**
   - Визуальные вкладки или кнопки для Markdown и HTML
   - Улучшить визуальное разделение панелей

2. **Улучшить дизайн:**
   - Иконки для кнопок переключения режимов
   - Более современный внешний вид

### Задача 5.2: Сохранение настроек пользователя

**Файлы для модификации:**
- `ThisAddIn.cs`
- `Properties/Settings.settings`

**Что нужно сделать:**

1. **Сохранять настройки:**
   - Режим отображения (Split/Markdown/HTML)
   - Размер панели
   - Видимость панели
   - Автосинхронизация (включена/выключена)

2. **Загружать настройки:**
   - Восстановление настроек при старте

---

## 📋 Этап 6: Тестирование и отладка

### Задача 6.1: Тестирование синхронизации

**Что нужно сделать:**

1. **Тестирование Сценария 1:**
   - Ввод текста в Word с Markdown-разметкой
   - Проверка синхронизации с Markdown-редактором
   - Проверка автоматического обновления HTML

2. **Тестирование Сценария 2:**
   - Ввод текста в Markdown-редакторе
   - Проверка автоматического обновления HTML
   - Проверка переноса в Word с форматированием

3. **Тестирование форматирования:**
   - Форматирование Markdown-синтаксиса в Word
   - Проверка корректности применения стилей

4. **Тестирование сохранения/загрузки:**
   - Сохранение настроек
   - Загрузка настроек при старте

### Задача 6.2: Обработка ошибок

**Что нужно сделать:**

1. **Обработка исключений:**
   - Добавить try-catch блоки во все новые методы
   - Логирование ошибок
   - Пользовательские сообщения об ошибках

2. **Валидация данных:**
   - Проверка входных данных
   - Обработка крайних случаев

---

## 📁 Структура новых файлов

```
Services/
  ├── WordToMarkdownConverter.cs      (Этап 2)
  ├── MarkdownToWordFormatter.cs      (Этап 3)
  └── WordMarkdownFormatter.cs        (Этап 4)

(Существующие файлы для модификации)
  ├── ThisAddIn.cs                    (Этап 2 - события Word)
  ├── Controls/TaskPaneControl.cs     (Этап 1, 2 - режимы, синхронизация)
  ├── MarkdownRibbon.cs               (Этап 3, 4 - новые кнопки)
  └── MarkdownRibbon.Designer.cs      (Этап 3, 4 - дизайн кнопок)
```

---

## 🎯 Последовательность реализации

**Рекомендуемый порядок выполнения:**

1. **Этап 1** - Переключение режимов отображения (быстрая визуальная часть)
2. **Этап 4** - Форматирование Markdown в Word (отдельная независимая функция)
3. **Этап 3** - Перенос Markdown → Word (логически проще, чем синхронизация)
4. **Этап 2** - Синхронизация Word → Markdown (самый сложный этап)
5. **Этап 5** - Улучшения и оптимизация
6. **Этап 6** - Тестирование на всех этапах

---

## 🔧 Ключевые технические детали

### Для синхронизации Word → Markdown:

- **События Word:**
  - Использовать `Application.WindowSelectionChange` для отслеживания выделения
  - Таймер для дебаунса изменений содержимого
  - `Document.Content.Text` для получения всего текста

- **Определение изменений:**
  - Сравнение текущего текста с предыдущим состоянием
  - Отслеживание только изменений (не полное перечитывание)

### Для форматирования Markdown в Word:

- **Поиск синтаксиса:**
  - Использовать `Range.Find` для поиска паттернов
  - Регулярные выражения для сложных паттернов

- **Применение стилей:**
  - `Range.Font` для форматирования шрифта
  - `Range.Style` для применения стилей Word
  - Удаление Markdown-синтаксиса после форматирования

### Для переноса Markdown → Word:

- **Парсинг Markdown:**
  - Использовать Markdig (уже в проекте) для парсинга
  - Получение AST (Abstract Syntax Tree) для преобразования

- **Создание стилей Word:**
  - Использовать встроенные стили Word
  - Применение форматирования построчно/поабзацно

---

## ✅ Чеклист готовности

### Этап 1 - Переключение режимов:
- [ ] Добавлены кнопки переключения в HTML
- [ ] Реализованы JavaScript функции переключения
- [ ] Добавлены методы в TaskPaneControl
- [ ] Сохранение настроек режима

### Этап 2 - Синхронизация Word → Markdown:
- [ ] Подписка на события Word
- [ ] Сервис WordToMarkdownConverter
- [ ] Автоматическое обновление Markdown-редактора
- [ ] Тестирование синхронизации

### Этап 3 - Перенос Markdown → Word:
- [ ] Кнопка "Перенести с форматированием"
- [ ] Кнопка "Перенести без изменений"
- [ ] Сервис MarkdownToWordFormatter
- [ ] Тестирование переноса

### Этап 4 - Форматирование в Word:
- [ ] Кнопка "Применить Markdown-разметку"
- [ ] Сервис WordMarkdownFormatter
- [ ] Парсинг Markdown-синтаксиса в тексте
- [ ] Применение стилей Word
- [ ] Тестирование форматирования

### Этап 5 - Улучшения:
- [ ] Улучшенный интерфейс панели
- [ ] Сохранение всех настроек
- [ ] Загрузка настроек при старте

### Этап 6 - Тестирование:
- [ ] Тестирование всех сценариев
- [ ] Обработка ошибок
- [ ] Валидация данных
- [ ] Финальная проверка функциональности

---

## 📝 Примечания

1. **Приоритет реализации:** Начать с простых задач (Этап 1, 4), затем переходить к более сложным (Этап 2).

2. **Тестирование:** Тестировать каждый этап сразу после реализации, не дожидаясь завершения всех этапов.

3. **Обратная совместимость:** Все изменения должны сохранять существующую функциональность.

4. **Производительность:** Учитывать производительность при синхронизации в реальном времени, использовать дебаунс и оптимизацию.

5. **Ошибки:** Все новые методы должны иметь обработку ошибок и логирование.

---

**Дата создания плана:** 2025-01-27  
**Версия плана:** 1.0  
**Статус:** Готов к реализации






















