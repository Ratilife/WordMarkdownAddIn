# Путеводитель по изучению проекта WordMarkdownAddIn

## 📋 Что нужно знать разработчику для изучения проекта

Этот документ описывает систематический подход к изучению незнакомого проекта на примере **WordMarkdownAddIn** — надстройки VSTO для Microsoft Word.

---

## 1. 🎯 ОБЩЕЕ ПОНИМАНИЕ ПРОЕКТА

### 1.1 Что это за проект?
**WordMarkdownAddIn** — это надстройка (Add-In) для Microsoft Word, которая позволяет:
- Редактировать Markdown-разметку в специальной панели задач
- Просматривать HTML-превью отформатированного Markdown
- Синхронизировать содержимое между Word-документом и Markdown
- Вставлять элементы Markdown через ленту Ribbon

### 1.2 Тип проекта
- **Технология**: VSTO (Visual Studio Tools for Office)
- **Язык**: C# (.NET Framework 4.8)
- **Платформа**: Windows (Microsoft Word)
- **Архитектура**: Desktop Add-In с веб-компонентами (WebView2)

### 1.3 Основные технологии и зависимости
- **VSTO** — для интеграции с Word
- **WebView2** — для отображения HTML/JavaScript редактора
- **Markdig** — библиотека для парсинга и рендеринга Markdown
- **Microsoft.Office.Interop.Word** — COM-интерфейсы Word

---

## 2. 📁 СТРУКТУРА ПРОЕКТА

### 2.1 Ключевые директории

```
WordMarkdownAddIn/
├── Controls/          # Пользовательские элементы управления
│   └── TaskPaneControl.cs  # Главный контрол панели с редактором
├── Services/          # Бизнес-логика и сервисы
│   ├── MarkdownRenderService.cs      # Рендеринг Markdown → HTML
│   ├── MarkdownToWordFormatter.cs    # Конвертация Markdown → Word
│   ├── WordToMarkdownService.cs      # Конвертация Word → Markdown
│   ├── DocumentSyncService.cs        # Синхронизация с документом
│   └── SyntaxHighlighter.cs          # Подсветка синтаксиса кода
├── docs/              # Документация проекта
│   ├── ThisAddIn.md                  # Описание главного модуля
│   ├── TaskPaneControl.md            # Описание контрола панели
│   └── plan/                          # Планы реализации
├── Properties/        # Настройки проекта
├── ThisAddIn.cs       # ТОЧКА ВХОДА - главный класс надстройки
├── MarkdownRibbon.cs  # Пользовательская лента Word
└── README.md          # Описание проекта
```

### 2.2 Ключевые файлы для изучения (в порядке приоритета)

1. **README.md** — общее описание проекта
2. **ThisAddIn.cs** — точка входа, жизненный цикл надстройки
3. **WordMarkdownAddIn.csproj** — зависимости и конфигурация
4. **Controls/TaskPaneControl.cs** — UI и логика редактора
5. **MarkdownRibbon.cs** — интерфейс ленты Word
6. **Services/** — бизнес-логика

---

## 3. 🔍 ЧТО ИЗУЧАТЬ ПЕРВЫМ ДЕЛОМ

### 3.1 Шаг 1: Понимание назначения
**Вопросы для ответа:**
- Что делает этот проект?
- Какую проблему решает?
- Кто целевая аудитория?

**Где искать:**
- `README.md`
- Комментарии в коде
- Документация в `docs/`

### 3.2 Шаг 2: Архитектура и структура
**Вопросы для ответа:**
- Какие основные компоненты?
- Как они взаимодействуют?
- Какие паттерны проектирования используются?

**Где искать:**
- `docs/UML-ClassDiagram.md` — диаграмма классов
- `docs/ThisAddIn.md` — описание архитектуры
- Структура папок и файлов

### 3.3 Шаг 3: Точка входа
**Вопросы для ответа:**
- Где начинается выполнение?
- Как инициализируется приложение?
- Какие события обрабатываются?

**Где искать:**
- `ThisAddIn.cs` — метод `ThisAddIn_Startup()`
- `ThisAddIn.cs` — метод `ThisAddIn_Shutdown()`

### 3.4 Шаг 4: Зависимости и технологии
**Вопросы для ответа:**
- Какие библиотеки используются?
- Какие версии?
- Есть ли конфликты зависимостей?

**Где искать:**
- `WordMarkdownAddIn.csproj` — секция `<ItemGroup><Reference>`
- `packages.config` — NuGet пакеты

---

## 4. 🧩 КЛЮЧЕВЫЕ КОМПОНЕНТЫ И ИХ НАЗНАЧЕНИЕ

### 4.1 ThisAddIn (главный класс)
**Роль**: Точка входа, координатор всех компонентов

**Основные обязанности:**
- Инициализация панели задач и ленты Ribbon
- Управление жизненным циклом надстройки
- Обработка событий Word (открытие/сохранение/закрытие документов)
- Сохранение/загрузка Markdown из документов

**Ключевые методы:**
- `ThisAddIn_Startup()` — инициализация
- `ThisAddIn_Shutdown()` — очистка ресурсов
- `EnsurePaneForWindow()` — создание панели для окна
- `LoadMarkdownFromDocument()` — загрузка Markdown
- `SaveMarkdownToDocument()` — сохранение Markdown

### 4.2 TaskPaneControl (контрол панели)
**Роль**: Пользовательский интерфейс редактора Markdown

**Основные обязанности:**
- Отображение редактора Markdown (через WebView2)
- Отображение HTML-превью
- Обработка изменений Markdown
- Управление режимами отображения (Split/Markdown/HTML)

**Ключевые компоненты:**
- `WebView2` — веб-компонент для отображения HTML/JS
- `MarkdownRenderService` — рендеринг Markdown → HTML
- JavaScript-редактор (встроенный в HTML)

### 4.3 MarkdownRibbon (лента Word)
**Роль**: Пользовательский интерфейс в ленте Word

**Основные обязанности:**
- Кнопки для вставки элементов Markdown
- Управление видимостью панели
- Сохранение/открытие файлов Markdown

**Ключевые кнопки:**
- `btnPanel` — показать/скрыть панель
- `btnSave` / `btnOpen` — работа с файлами
- `bBold`, `bItalic`, `bH1`, `bH2` и т.д. — форматирование

### 4.4 Services (сервисы)
**Роль**: Бизнес-логика и обработка данных

**Основные сервисы:**
- **MarkdownRenderService** — конвертация Markdown → HTML
- **MarkdownToWordFormatter** — вставка Markdown-элементов в Word
- **WordToMarkdownService** — конвертация Word → Markdown
- **DocumentSyncService** — синхронизация с документом
- **SyntaxHighlighter** — подсветка синтаксиса кода

---

## 5. 🔄 ПОТОК ДАННЫХ И ВЗАИМОДЕЙСТВИЕ

### 5.1 Жизненный цикл надстройки

```
Запуск Word
    ↓
ThisAddIn_Startup()
    ↓
Создание панели задач (TaskPaneControl)
    ↓
Инициализация WebView2
    ↓
Загрузка Markdown из документа (если есть)
    ↓
Отображение редактора
    ↓
[Пользователь редактирует Markdown]
    ↓
Изменения → MarkdownRenderService → HTML
    ↓
Отображение HTML-превью
    ↓
Сохранение документа → SaveMarkdownToDocument()
    ↓
Закрытие Word
    ↓
ThisAddIn_Shutdown()
```

### 5.2 Поток данных Markdown → HTML

```
Пользователь вводит Markdown (в WebView2)
    ↓
JavaScript отправляет сообщение в C# (WebMessageReceived)
    ↓
TaskPaneControl.CoreWebView2_WebMessageReceived()
    ↓
MarkdownRenderService.RenderoHtml()
    ↓
HTML строка
    ↓
PostRenderHtml() → ExecuteScriptAsync()
    ↓
Отображение в WebView2
```

### 5.3 Синхронизация с документом Word

```
Сохранение документа
    ↓
Application_DocumentBeforeSave()
    ↓
GetCachedMarkdown() из TaskPaneControl
    ↓
SaveMarkdownToDocument() → CustomXMLParts
    ↓
XML-фрагмент встроен в документ
```

---

## 6. 🛠️ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### 6.1 Хранение Markdown в документе
- **Механизм**: CustomXMLParts (встроенные XML-фрагменты)
- **Формат**: XML с пространством имён `DocumentSyncService.NamespaceUri`
- **Структура**: `<md:markdown><md:content><![CDATA[...]]></md:content></md:markdown>`

### 6.2 Коммуникация C# ↔ JavaScript
- **Механизм**: WebView2 WebMessageReceived
- **Формат**: `"тип|данныеВBase64"`
- **Примеры типов**: `"mdChanged"`, `"viewModeChanged"`

### 6.3 Управление панелями для нескольких окон
- **Проблема**: Word может открыть несколько документов одновременно
- **Решение**: Словари `_markdownPanes` и `_paneControls` с ключом `Word.Window`
- **Каждое окно** имеет свою панель и контрол

---

## 7. 📚 ДОКУМЕНТАЦИЯ В ПРОЕКТЕ

### 7.1 Где искать документацию

1. **docs/ThisAddIn.md** — описание главного модуля
2. **docs/TaskPaneControl.md** — описание контрола панели
3. **docs/MarkdownRibbon.md** — описание ленты
4. **docs/UML-ClassDiagram.md** — диаграмма классов
5. **docs/plan/** — планы реализации и инструкции

### 7.2 Полезные диаграммы
- `docs/UML-ClassDiagram.puml` — PlantUML диаграмма классов
- `docs/TaskPaneControl_FlowDiagram.md` — диаграмма потока
- `docs/ThisAddIn-CallTree.txt` — дерево вызовов

---

## 8. 🐛 ИЗВЕСТНЫЕ ПРОБЛЕМЫ И ОГРАНИЧЕНИЯ

### 8.1 Что нужно доработать (из docs/ThisAddIn.md)
- ❌ Улучшить обработку исключений (везде `catch { }`)
- ❌ Восстановление состояния панели при старте
- ❌ Синхронизация при переключении документов
- ❌ Потенциальная утечка COM-объектов

### 8.2 Где искать информацию о проблемах
- `docs/ThisAddIn.md` — раздел "Что нужно дописать"
- `АНАЛИЗ_ПРОБЛЕМЫ_*.md` — файлы с анализом конкретных проблем
- Комментарии в коде с пометками `TODO` или `FIXME`

---

## 9. 🚀 КАК НАЧАТЬ РАБОТУ С ПРОЕКТОМ

### 9.1 Первые шаги

1. **Прочитать README.md**
2. **Изучить структуру проекта** (список файлов)
3. **Открыть ThisAddIn.cs** и прочитать комментарии
4. **Просмотреть диаграмму классов** (`docs/UML-ClassDiagram.md`)
5. **Запустить проект** и посмотреть, как он работает

### 9.2 Для понимания конкретной функциональности

**Хочу понять, как работает редактор:**
→ Изучить `Controls/TaskPaneControl.cs`

**Хочу понять, как сохраняется Markdown:**
→ Изучить `ThisAddIn.cs` → методы `SaveMarkdownToDocument()` и `LoadMarkdownFromDocument()`

**Хочу понять, как работает рендеринг:**
→ Изучить `Services/MarkdownRenderService.cs`

**Хочу понять, как вставляются элементы в Word:**
→ Изучить `Services/MarkdownToWordFormatter.cs`

### 9.3 Для добавления новой функциональности

1. Определить, в какой компонент добавить функциональность
2. Изучить существующий код этого компонента
3. Посмотреть примеры похожей функциональности
4. Изучить документацию в `docs/`
5. Проверить планы в `docs/plan/`

---

## 10. ✅ ЧЕКЛИСТ ДЛЯ ПОЛНОГО ПОНИМАНИЯ ПРОЕКТА

- [ ] Понимаю назначение проекта
- [ ] Знаю основные компоненты и их роли
- [ ] Понимаю точку входа (`ThisAddIn_Startup`)
- [ ] Знаю, как работает жизненный цикл надстройки
- [ ] Понимаю поток данных Markdown → HTML
- [ ] Знаю, как сохраняется/загружается Markdown
- [ ] Понимаю взаимодействие C# ↔ JavaScript
- [ ] Знаю структуру проекта и расположение файлов
- [ ] Понимаю используемые технологии и зависимости
- [ ] Знаю известные проблемы и ограничения
- [ ] Могу найти нужный код для конкретной задачи
- [ ] Понимаю архитектуру и паттерны проектирования

---

## 11. 📖 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

### 11.1 Технологии, которые нужно знать
- **VSTO** — Visual Studio Tools for Office
- **WebView2** — Microsoft Edge WebView2
- **Markdown** — синтаксис разметки
- **COM Interop** — взаимодействие с Word через COM
- **CustomXMLParts** — встроенные XML-фрагменты в Word

### 11.2 Полезные ссылки (для изучения технологий)
- Документация VSTO
- Документация WebView2
- Документация Markdig
- Документация Word Interop

---

## 12. 💡 СОВЕТЫ ПО ИЗУЧЕНИЮ

1. **Начните с общего** — сначала поймите назначение, потом детали
2. **Используйте документацию** — в проекте есть хорошая документация в `docs/`
3. **Читайте комментарии** — код хорошо прокомментирован
4. **Запускайте проект** — смотрите, как он работает
5. **Изучайте по частям** — не пытайтесь понять всё сразу
6. **Используйте отладчик** — ставьте breakpoints и смотрите поток выполнения
7. **Читайте связанные файлы** — если видите вызов метода, посмотрите его реализацию

---

## Заключение

Этот проект — хороший пример надстройки VSTO с современными технологиями (WebView2). Для его изучения важно:
1. Понять общую архитектуру
2. Изучить точку входа и жизненный цикл
3. Разобраться в потоке данных
4. Изучить ключевые компоненты по отдельности

Удачи в изучении! 🚀








